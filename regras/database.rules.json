{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "medications": {
          ".indexOn": ["name", "nextDoseTime"]
        },
        "profiles": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "history": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid",
          ".indexOn": ["timestamp", "medicationId"]
        }
      }
    },
    "caregiverAlerts": {
      "$caregiverUid": {
        ".read": "$caregiverUid === auth.uid",
        "$alertId": {
          ".write": "auth != null && newData.child('patientUid').val() === auth.uid"
        }
      }
    },
    "master_catalog": {
      ".read": "auth != null",
      ".write": "root.child('users').child(auth.uid).child('isAdmin').val() === true"
    }
  }
}
